# Pipeline Import / Export Example

This directory contains a synthetic pipeline artifact that follows the public draft schema.

## Validate Conceptually

The artifact is designed for documentation and schema feedback. It does not execute proprietary BCILattice logic.
